define(['bootstrap/app'], function(app) {
    'use strict';

    app.controller('region-controller', ['$rootScope', '$scope', '$state', function($rootScope, $scope, $state) {

        // 代码组织规则：
        // 1、所有在 View 中绑定用到的变量，都在最开始设置初始值。以便于了解有哪些绑定的变量，同时避免没有设置初始值可能引起的问题。




        
    }]);
});